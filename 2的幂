/**
 * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
 *
 * 
 * @param n int整型 
 * @return bool布尔型
 */
 //n一直除以2 根据过程中是否恰好为整除 并且 保证最终的结果为1
bool poweroftwo(int n ) {
    // write code here
    if(n%2!=0 && n!=1)
       return false;
    while(n>0 && n%2==0){
         n=n/2;
    }
    if(n==1)
      return true;
    return false;
}
