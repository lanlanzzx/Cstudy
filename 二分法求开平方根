    // int left=0;
    // int mid=0;
    // int right=x;
    // while(left<=right){
    //      mid=(left+right)/2;
    //      if(mid*mid<=x && (mid+1)*(mid+1)>x) return mid;
    //          //right=x-1;
    //      if(mid*mid<x)
    //           left=mid+1;
    //      if((mid)*(mid)>x)
    //         right=mid-1;
    //         //return mid;
    // }
    // return 0;
